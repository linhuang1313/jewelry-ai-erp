# Railway Nixpacks 配置
providers = ["python"]

[variables]
PYTHON_VERSION = "3.10"

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"

